<h1><%= t 'views.head.edit', name: Book.model_name.human.downcase %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t('views.nav.show', name: Book.model_name.human.downcase), @book %> |
  <%= link_to t('views.nav.back', name: ja_pluralize(Book.model_name.human.downcase) ), books_path %>
</nav>
